<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.AvisaAqui" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Cores principais -->
        <item name="colorPrimary">@color/primaryPurple</item>
        <item name="colorPrimaryVariant">@color/lightPurple</item>
        <item name="colorOnPrimary">@color/textBeige</item>

        <item name="colorSecondary">@color/accentGreen</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Cor de fundo padrão -->
        <item name="android:colorBackground">@color/white</item>

        <!-- Personalização de fontes e ícones -->
        <item name="android:textColor">@color/textBeige</item>

        <!-- Controlar cor de status bar -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/primaryPurple</item>
    </style>
</resources>
